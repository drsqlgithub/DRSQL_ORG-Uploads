A few objects that are there to replace some functionality in memory optimized objects

String$CheckSimpleAlphaNumeric - replacement for a particular usage of like that is not available in some uses. Looks for any data that isn't simple A-Z, 0-9

String$Replicate - works like replicate, which is not in memory optimized coe