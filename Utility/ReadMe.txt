The Utility schema is designed for code that is generally for administration of the server.

Code in this directory is designed to be in a schema named Utility. If you need to change to something else, just replace [Utility] with your desired schema.

Each file also includes a create for the schema.